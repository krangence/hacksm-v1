config:
  sharedSecret: "$POMERIUM_SHARED_SECRET"
  cookieSecret: "$POMERIUM_COOKIE_SECRET"
authenticate:
  idp:
    provider: github
    clientID: "$POMERIUM_CLIENT_ID"
    clientSecret: "$POMERIUM_CLIENT_SECRET"
    serviceAccount: "$POMERIUM_SERVICE_ACCOUNT"
