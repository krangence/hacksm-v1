---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: external-dns
  namespace: external-dns
spec:
  releaseName: external-dns
  chart:
    repository: https://charts.bitnami.com/bitnami
    name: external-dns
    version: 3.4.2
  valuesFrom:
    - secretKeyRef:
        name: external-dns-values
  values:
    domainFilters: ["hacksm.net"]
    policy: sync
    aws:
      credentials:
        accessKey: AKIAW4YYMLNFPY4BD76X
    logLevel: debug
